import { privateRobots } from "@/lib/seo";

export const metadata = {
  title: "Criar conta de assinatura",
  description: "Crie sua conta no Social Jurídico Assinatura.",
  robots: privateRobots,
};

export default function SignatureSignupLayout({ children }) {
  return children;
}

