import { privateRobots } from "@/lib/seo";

export const metadata = {
  title: "Atualizar senha",
  robots: privateRobots,
};

export default function AtualizarSenhaLayout({ children }) {
  return children;
}
