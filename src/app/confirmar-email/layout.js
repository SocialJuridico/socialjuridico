import { privateRobots } from "@/lib/seo";

export const metadata = {
  title: "Confirmar e-mail",
  robots: privateRobots,
};

export default function ConfirmarEmailLayout({ children }) {
  return children;
}
